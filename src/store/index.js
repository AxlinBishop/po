import { createStore } from 'vuex'
import proyectos from '../db/proyectos.json'

export default createStore({
  state: {
    proyectos: proyectos 
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
